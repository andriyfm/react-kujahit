import React from "react";
import Layout from "../components/Layout";
import CTA from "../components/cta";

const Home = props => {
  return (
    <Layout>
      <CTA />
    </Layout>
  );
};

export default Home;
